{
  "name": "server",
  "version": "1.0.0",
  "description": "Atttendance server",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "attendance"
  ],
  "author": "Richard Correa",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "tslog": "^3.3.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "jest": "^28.1.1",
    "supertest": "^6.2.3",
    "tslint": "^6.1.3",
    "typescript": "^4.7.4"
  }
}
